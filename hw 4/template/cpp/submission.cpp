// Submit this file to Gradescope
#include "submission.h"

using std::vector;

vector<int> Solution::HclusSingleLink(const vector<vector<double> > &X, int K) {
  // Single link hierarchical clustering
  // Args:
  //   - X: 2D data points
  //   - K: the number of output clusters
  // Returns:
  //   A list of integers (range from 0 to K - 1) that represent class labels.
  //   The number does not matter as long as the clusters are correct.
  //   For example: [0, 0, 1] is treated the same as [1, 1, 0]"""
  // implement this method
}

vector<int> Solution::HclusCompleteLink(const vector<vector<double> > &X, int K) {
  // implement this method
}

vector<int> Solution::HclusAverageLink(const vector<vector<double> > &X, int K) {
  // implement this method
}
